package game.entity.esm;

public enum EntityState {
    STATE_WALKING,
    STATE_STUNNED,
    STATE_DASHING,
    STATE_JUMPING,
    STATE_STANDING,
    STATE_DEAD,
    STATE_ATTACKING,
    STATE_BLOCKING
}
