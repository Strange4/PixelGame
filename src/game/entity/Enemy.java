package game.entity;

import game.graphics.SpriteSheet;
import game.util.Vector2D;

public class Enemy extends Entity{
    public Enemy(SpriteSheet spriteSheet, Vector2D position) {
        super(spriteSheet, position);
    }
}
